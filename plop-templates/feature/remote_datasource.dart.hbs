import '../../../../../core/error/exception.dart';
import '../../../domain/entities/{{snakeCase name}}/{{snakeCase name}}.dart';

abstract class {{pascalCase name}}RemoteDataSource {
  Future<List<{{pascalCase name}}>> getAll{{pascalCase name}}s();
}

class {{pascalCase name}}RemoteDataSourceImpl implements {{pascalCase name}}RemoteDataSource {
  @override
  Future<List<{{pascalCase name}}>> getAll{{pascalCase name}}s() async {
    try {
      // Simulate API call
      await Future<void>.delayed(const Duration(seconds: 1));
      return <{{pascalCase name}}>[
        {{pascalCase name}}(
          id: '1',
          displayName: 'Example {{pascalCase name}} 1',
          description: 'This is an example {{pascalCase name}}',
          address: '123 Main St',
          localizedCategory: 'Category 1',
          phoneNumber: '+1234567890',
          website: 'https://example1.com',
          latitude: 40.7128,
          longitude: -74.0060,
        ),
        {{pascalCase name}}(
          id: '2',
          displayName: 'Example {{pascalCase name}} 2',
          description: 'Another example {{pascalCase name}}',
          address: '456 Second Ave',
          localizedCategory: 'Category 2',
          phoneNumber: '+0987654321',
          website: 'https://example2.com',
          latitude: 34.0522,
          longitude: -118.2437,
        ),
      ];
    } catch (e) {
      throw ServerException(e.toString());
    }
  }
} 